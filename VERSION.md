# 贪吃蛇（tanchishe）版本说明

## 版本 2.2（当前）

> 发布日期：2026-08-27
> 分支：`utf8-encoding`

### 本版新增

| 类别 | 内容 |
|---|---|
| ✨ 新增 | 启动首屏登录窗口：输入用户名+密码；新用户名自动注册，老用户校验密码（SHA-256 加盐存储，不落明文） |
| ✨ 新增 | 本地玩家档案：%USERPROFILE%/.tanchishe/players.txt，每局成绩带时间戳自动落盘 |
| ✨ 新增 | 排行榜：登录窗右侧按最高分展示前 10 名；点击玩家弹窗查看总局数与逐局成绩（最新在前，保留最近 50 局） |
| 🔧 变更 | 程序入口改为先登录再游戏，游戏窗口标题显示当前玩家名 |
| 📦 数据 | 档案写入采用临时文件+原子移动，防写坏；损坏行自动跳过不影响其它玩家 |

### 新增类

- `LoginFrame` — 登录/注册窗口 + 排行榜面板（JList 点击查看详情）
- `PlayerStore` — 玩家档案持久化（Tab 分隔文本，SHA-256+salt 口令，纯 JDK 无外部依赖）

---

## 版本 2.1

> 发布日期：2026-08-27
> 分支：`utf8-encoding`（提交 `183a03f`）

### 本版新增 / 修复

| 类别 | 内容 |
|---|---|
| 🐛 修复 | 反向判定竞态：重开游戏后残留旧方向，开局瞬间按方向键可能被反向保护放行而秒死 |
| 🐛 修复 | 方向键乱按（一拍内连按两个方向）导致蛇回头咬到自己、游戏误结束 |
| 🐛 修复 | 食物刷新可能落在蛇身上，被"凭空吃掉" |
| 🐛 修复 | 吃到食物瞬间尾巴闪跳一帧 |
| 🔧 重构 | 方向输入改为队列机制（每拍消费一个方向，入队时校验反向，杜绝回头）；彻底移除旧 `fxed` 字段 |
| 🔧 加固 | 蛇长达到数组上限（750）时只加分不变长，防止越界崩溃 |
| 🔧 加固 | 方向比较全部改用 `equals()`，消除 `==` 字符串比较隐患 |
| 📦 工程 | 源码统一编码为 UTF-8（原 GBK），新增 Eclipse 项目级编码设置 |

### 操作说明

- 空格：开始 / 暂停 / 死亡后重新开始
- 方向键：控制蛇的移动方向
- 蛇头碰到自己身体则游戏结束
- 边界为穿越模式（从一侧出去会从另一侧回来）

### 运行方式

```bash
# 编译（源码为 UTF-8）
javac -encoding UTF-8 -d bin src/Msnake.java src/MPanel.java

# 运行
java -cp bin Msnake
```

---

## 版本 2.0（基线）

> 提交：`7912e91`（utf8-encoding 分支早期修复集）

- 修复：重开残留方向、食物重叠、吃食尾巴闪跳、死代码清理
- 此版本为 2.1 的上一个开发基线

## 版本 1.x（历史）

> 提交：`5edba64`（main 分支原始版本）

- 原始 GBK 编码版本，Eclipse 工程（工程名 Msnake）
- 基础贪吃蛇玩法：图片素材、背景音乐、计时器驱动
